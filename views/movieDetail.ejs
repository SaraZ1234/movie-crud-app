<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Movie Details</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .movie-card {
      background: #ffffff;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      padding: 35px 30px;
      max-width: 420px;
      width: 100%;
      transition: transform 0.3s ease;
    }

    .movie-card:hover {
      transform: translateY(-5px);
    }

    .movie-card h2 {
      color: #333;
      font-size: 26px;
      margin-bottom: 15px;
      border-bottom: 2px solid #f0f0f0;
      padding-bottom: 10px;
    }

    .movie-card p {
      color: #555;
      font-size: 16px;
      margin: 8px 0;
      line-height: 1.5;
    }

    .movie-card p strong {
      color: #111;
    }

    .actions {
      margin-top: 25px;
      display: flex;
      justify-content: flex-start;
      gap: 20px;
    }

    .actions a {
      background: #e9e9e9;
      padding: 10px 14px;
      border-radius: 8px;
      color: #444;
      font-size: 18px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s ease;
    }

    .actions a:hover {
      background: #007BFF;
      color: #fff;
    }

    .actions a:last-child:hover {
      background: #e63946;
    }

    .actions i {
      pointer-events: none;
    }

    @media (max-width: 480px) {
      .movie-card {
        padding: 25px 20px;
      }

      .movie-card h2 {
        font-size: 22px;
      }

      .movie-card p {
        font-size: 15px;
      }

      .actions a {
        font-size: 16px;
        padding: 8px 12px;
      }
    }
  </style>
</head>
<body>

  <div class="movie-card">
    <h2><%= home.title %></h2>
    <p><strong>Director:</strong> <%= home.director %></p>
    <p><strong>IMDb:</strong> <%= home.imdb %></p>
    <p><strong>Rating:</strong> <%= home.rating %></p>
    <p><strong>Year:</strong> <%= home.year %></p>

    <div class="actions">
      <a href="edit.html" title="Edit">
        <i class="fas fa-pencil-alt"></i>
      </a>
      <a href="delete.html" title="Delete">
        <i class="fas fa-trash-alt"></i>
      </a>
    </div>
  </div>

</body>
</html>
